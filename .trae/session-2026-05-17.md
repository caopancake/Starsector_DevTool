# Session Summary — 2026-05-17

## 本次完成的工作

### 1. 修复贴图缺失问题（主表格 sprite 加载）

**问题**：主数据表格（舰船/武器/船插/工业）中引用 starsector-core 图片的记录在 DetailPane 显示"贴图缺失"。

**根因**：`src-tauri/src/services/project/sprites.rs` 的 `load_sprite_data_url` 只检查 Mod 目录，未 fallback 到 starsector-core。

**修复**：
- `sprites.rs`：`load_sprite_data_url` 新增 `core_dir: Option<&Path>` 参数，实现 Mod → core 的 fallback 链
- `sprites.rs`：所有公开函数（`load_ship_sprite_data`、`load_weapon_sprite_data`、`load_hullmod_sprite_data`、`load_industry_sprite_data`）统一新增 `core_dir` 参数
- `project/mod.rs`：4 处调用点传入 `core_dir.as_deref()`
- 全部通过 clippy + fmt + 29 tests

### 2. 新增 Phase 17: 可视化逻辑编辑器（蓝图系统）

基于 Starsector Java 模块高度模板化的观察，规划了节点式可视化逻辑编辑器：
- Phase 17.1: 模板向导（纯表单代码生成）
- Phase 17.2: 对话流编辑器（rules.csv + BarEvent 可视化）
- Phase 17.3: 效果蓝图编辑器（Ship System / Hullmod 节点图）
- Phase 17.4: Starsector API 节点库

集成库：MagicLib、GraphicsLib、LazyLib、LunaLib、BoxUtil。

### 3. 新增 Phase 18: 社区库数据文件集成

分析了 MagicLib / GraphicsLib / LazyLib 的数据文件格式，确定了可直接嵌入现有框架的部分：

**直接可做（零新基础设施）：**
- `ship_systems.csv` 加入主表格（改 CSV_TABLES）
- `data/config/*.json` 加入可编辑文件列表（MagicLib modSettings）
- `data/lights/*.csv` 加入 campaign CSV 扫描（GraphicsLib）

**需少量扩展：**
- `magic-bounty.schema.json` + 赏金列表编辑器
- `ship-system.schema.json` + .system JSON 编辑器
- 依赖检测（mod_info.json dependencies 条件暴露入口）
- Hjson 解析适配

**需新增基础设施：**
- CSV 列 Schema 系统（为 CSV 列提供类型注解 → 富控件渲染）

## 更新的文档

| 文件 | 内容 |
|------|------|
| `.trae/todo.md` | 新增 Phase 17（蓝图系统）+ Phase 18（社区库数据集成） |
| `.trae/module-map.md` | 新增 Blueprint System 规划区块 + Community Library Integration 区块 |
| `.trae/backend-guidelines.md` | 新增代码生成规则 + sprite fallback 说明更新 |
| `.trae/frontend-guidelines.md` | 新增蓝图编辑器规则 |
| `.trae/terminology.md` | 新增蓝图系统术语 + 社区库术语（MagicLib/GraphicsLib/LazyLib/LunaLib/BoxUtil） |

## 关键技术决策记录

1. **Starsector 四大系统（空间站/技能/任务/对话）均需 Java** — 纯数据驱动无法覆盖，蓝图系统的价值在于把 80% 常见模式降低到"填表+连线"
2. **LazyLib 无数据文件** — 纯 Java 运行时工具库，当前阶段无需集成
3. **MagicLib 的 magicBounty_data.json 用 Hjson 格式** — 与现有 starsector_json 宽松解析器兼容性待确认
4. **GraphicsLib 通过 CSV 配置** — 完全可复用 CampaignView 编辑模式
5. **Phase 18 优先于 Phase 17** — 数据文件集成工作量小、价值即时，蓝图系统是远期大工程

## 下次对话可立即开始的工作

按优先级：
1. **Phase 7-10**（数据校验、快捷键、右键菜单、硬化）— 核心功能完善
2. **Phase 18.1**（ship_systems.csv + 扫描范围扩展）— 1-2 天即可完成
3. **Phase 11-16**（技能/船皮/变体/战役扩展/角色/高级配置）— 编辑器功能扩展
4. **Phase 17**（蓝图系统）— 远期大工程

## 项目当前状态

- Phase 1-2: 完成（舰船编辑器 + 多 Mod 工作区）
- Phase 3: 未开始（主题强调色）
- Phase 4: 完成（全局 undo/redo）
- Phase 5: 完成（配置模块）
- Phase 6: 大部分完成（Schema Registry + core 扫描，CSV 列 Schema 待做）
- Phase 7-16: 未开始
- Phase 17-18: 已规划，未开始
