# Todo

后续工作按 phase 推进。每个 phase 完成后应更新本文档。

## Phase 1: 修复数据一致性和迁移残留

- [x] 调整表格新建流程：后端写入成功后再提交前端状态，或失败时完整回滚本地状态。
- [x] 调整表格删除流程：后端删除成功后再提交前端状态，或失败时完整回滚本地状态。
- [x] 统一 CSV round-trip 模型，确保保存和删除对表头、注释行、空字段的处理一致。
- [x] 删除 `editors.store.ts` 中无实际用途的 `weaponCsvRow` 迁移残留。
- [x] 为新建失败、删除失败、CSV 注释保留增加回归测试或最小验证用例。


## Phase 1.5: 易用性优化
- [x] 右侧详情面板添加缩略图预览（舰船、武器、船插显示贴图，联队和工业显示占位符）。
- [ ] 
- [ ] 
- [ ] 

## Phase 2: 收敛应用壳职责和局部 DOM 异味

- [ ] 从 `App.vue` 拆出主表格视图，让应用壳不直接承载表格渲染细节。
- [ ] 从 `App.vue` 拆出右侧记录详情面板，让记录操作入口有稳定组件边界。
- [ ] 从 `App.vue` 拆出编辑器宿主组件，集中管理舰船、武器、弹丸和预览弹窗。
- [ ] 移除 `document.querySelector('.cell-input')` 聚焦逻辑，改为 Vue ref 或局部 cell editor 组件。
- [ ] 确认拆分后打开项目、编辑单元格、保存、删除、打开编辑器链路不回归。

## Phase 3: 收敛后端加载链路和保存语义边界

- [ ] 拆分 `services/project.rs`，把阵营发现、projectile fallback、sprite 加载和项目装配分离为私有 helper 模块。
- [ ] 保留 `editor.service.ts` 和 `table.service.ts` 作为 feature 边界，但补充实际语义，例如错误文案、保存前 normalize 或上下文约束。
- [ ] 明确 UI 和文档中的保存边界：顶部保存只保存 CSV，编辑器保存只保存 `.ship/.wpn/.proj`。
- [ ] 评估是否需要 `.ship` 字段和 CSV 字段联动；如需要，先设计同步规则再实现。
- [ ] 为后端项目加载拆分后的 faction/projectile/sprite 路径补最小测试或验证样例。

## Phase 4: 结束迁移状态并删除旧程序

- [ ] 彻底删除 `old_program/` 目录。
- [ ] 从 `AGENTS.md`、`README.md`、`.trae/` 文档中移除 `old_program` 引用。
- [ ] 从项目文档中移除“旧版参考”“迁移参考”等历史叙述。
- [ ] 清理配置和脚本中针对 `old_program/` 的 ignore 或跳过逻辑。
- [ ] 验证删除后编码检查、格式检查、前端检查和 Rust 检查仍通过。

## Phase 5: 重新整理项目 CSS

- [ ] 明确哪些样式保留为 global token、reset 和布局基础。
- [ ] 决定模块样式按页面聚合还是按 feature 聚合。
- [ ] 拆分或重组当前 `src/styles.css`，避免继续膨胀。
- [ ] 建立颜色、间距、边框、阴影、圆角的基础 token。
- [ ] 验证表格、右侧面板、弹窗、编辑器滚动区域不回归。

## Phase 6: 重新设计前端界面（允许重构语义）

- [ ] 定义 Native UI + 现代圆角 + Notion 风格极简的视觉基准。
- [ ] 重设主窗口导航、顶部栏、数据表和右侧详情面板。
- [ ] 重设舰船、武器、弹丸编辑器弹窗布局。
- [ ] 重设颜色、层级、hover、selected、dirty、disabled 状态。
- [ ] 验证高密度数据工具场景下可读性和操作效率。

## Phase 7: 重新梳理项目链路/确认板块

- [ ] 确认应用内模块分块是否合理：project、tables、editors、preview。
- [ ] 确认弹窗、抽屉、右侧详情面板之间的职责边界。
- [ ] 梳理打开项目、切换表、选择记录、打开编辑器、保存的完整链路。
- [ ] 确认 store、service、component、composable 是否仍符合稳定语义。
- [ ] 删除或合并不再提供维护价值的中间层。

## Phase 8: 建立完整修改链路和全局历史

- [ ] 定义统一修改事件模型，覆盖 CSV 单元格、记录新建/删除、`.ship/.wpn/.proj` 编辑、贴图字段写入等操作。
- [ ] 建立全局 undo/redo 历史栈，允许连续记忆所有可撤销修改。
- [ ] 设计跨页面/跨弹窗撤销重做规则，明确当前页面关闭、切换模块、重新打开编辑器后的行为。
- [ ] 明确哪些操作不可撤销，例如已经写入磁盘的删除、覆盖贴图、外部文件变化，并在 UI 中给出清晰反馈。
- [ ] 将现有编辑器局部 history 接入统一修改链路，避免多个互不相通的撤销栈。
- [ ] 为跨页面撤销、连续重做、保存后历史处理补回归测试或最小手动验收流程。

## Phase 9: 重新梳理快捷键

- [ ] 参考 `sf-ship-ed` 的快捷键习惯，整理舰船编辑器快捷键。
- [ ] 参考 `sf-ship-ed` 的快捷键习惯，整理武器编辑器快捷键。
- [ ] 为弹丸编辑器和弹道预览定义必要快捷键。
- [ ] 在不增加学习成本的前提下统一撤销、重做、保存、关闭等通用行为，并接入 Phase 8 的全局历史链路。
- [ ] 避免快捷键和输入框、文本域、系统快捷键冲突。

## Phase 10: 定义右键行为

- [ ] 定义主表格右键菜单：复制 ID、打开编辑器、删除、定位资源等。
- [ ] 定义舰船画布右键行为：添加点、删除点、切换模式、复制坐标等。
- [ ] 定义武器画布右键行为：添加 barrel、删除 barrel、复制坐标等。
- [ ] 定义弹丸编辑器右键行为：复制字段、重置字段、定位贴图等。
- [ ] 确保右键菜单不会破坏画布右键拖动平移体验。

## Phase 11: 自动数据校验和警示

- [ ] 建立统一数据校验入口，覆盖 CSV 表格、`.ship`、`.wpn`、`.proj` 和贴图资源。
- [ ] 对贴图宽度或高度为奇数的资源给出警示。
- [ ] 对坐标类字段含小数点的情况给出警示，包括武器位置、引擎位置、边界点、舰船中心、护盾位置、炮口位置等。
- [ ] 对缺失舰船中心或护盾位置的舰船给出警示。
- [ ] 对没有炮口/barrel offset 的武器给出警示。
- [ ] 对 `collisionRadius` 小于 `shieldRadius` 的舰船给出警示。
- [ ] 对只能填数值的 CSV 表格列填入非数值的情况给出警示。
- [ ] 设计校验结果展示位置：表格行级标记、右侧详情提示、编辑器内字段提示和保存前汇总。
- [ ] 明确警示与阻止保存的边界；默认先警示，不轻易阻止保存。
- [ ] 为典型离谱数据样例补最小测试或手动验收清单。

## Phase 12: 多 Mod 支持和持久化

- [ ] 支持导入并同时管理多个 Starsector mod 根目录。
- [ ] 设计 mod 切换入口，优先考虑最左侧导航或项目选择区域。
- [ ] 为每个 mod 独立维护表格数据、编辑器状态、筛选条件、dirty state 和校验结果。
- [ ] 设计持久化存储，记录已打开的 mod 列表、最近选中的 mod 和可持续保存的用户设置。
- [ ] 明确持久化位置和格式，避免写入 mod 目录内的工具私有状态。
- [ ] 处理 mod 不存在、路径移动、权限不足、重复导入等异常情况。
- [ ] 切换 mod 时提示未保存修改，避免跨 mod 丢失 dirty state。
- [ ] 为多 mod 打开、切换、移除、恢复上次会话补最小测试或手动验收流程。

## Phase 13: 游戏全量读取

- [ ] 支持指定 Starsector 游戏根目录，而不仅是单个 mod 根目录。
- [ ] 自动识别原版 `starsector-core` 数据和 `mods/` 下所有可用 mod。
- [ ] 建立游戏级项目模型，区分原版数据、每个 mod 数据和当前可编辑目标。
- [ ] 支持在 UI 中切换原版数据、不同 mod、合并视图或对照视图。
- [ ] 明确原版数据默认只读，避免误写 `starsector-core`。
- [ ] 处理未启用 mod、缺失 `mod_info.json`、重复 id、依赖关系和加载顺序等情况。
- [ ] 为武器、弹丸、舰船等跨 mod 引用提供 fallback 和来源标识。
- [ ] 持久化最近打开的游戏目录、mod 选择状态和用户视图设置。
- [ ] 为游戏目录扫描、原版 fallback、多 mod 冲突和切换编辑目标补最小测试或手动验收流程。
