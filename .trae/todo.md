# Todo

后续工作按 phase 推进。每个 phase 完成后应更新本文档。

## Phase 1: 修复数据一致性和迁移残留

- [ ] 调整表格新建流程：后端写入成功后再提交前端状态，或失败时完整回滚本地状态。
- [ ] 调整表格删除流程：后端删除成功后再提交前端状态，或失败时完整回滚本地状态。
- [ ] 统一 CSV round-trip 模型，确保保存和删除对表头、注释行、空字段的处理一致。
- [ ] 删除 `editors.store.ts` 中无实际用途的 `weaponCsvRow` 迁移残留。
- [ ] 为新建失败、删除失败、CSV 注释保留增加回归测试或最小验证用例。

## Phase 2: 收敛应用壳职责和局部 DOM 异味

- [ ] 从 `App.vue` 拆出主表格视图，让应用壳不直接承载表格渲染细节。
- [ ] 从 `App.vue` 拆出右侧记录详情面板，让记录操作入口有稳定组件边界。
- [ ] 从 `App.vue` 拆出编辑器宿主组件，集中管理舰船、武器、弹丸和预览弹窗。
- [ ] 移除 `document.querySelector('.cell-input')` 聚焦逻辑，改为 Vue ref 或局部 cell editor 组件。
- [ ] 确认拆分后打开项目、编辑单元格、保存、删除、打开编辑器链路不回归。

## Phase 3: 收敛后端加载链路和保存语义边界

- [ ] 拆分 `services/project.rs`，把阵营发现、projectile fallback、sprite 加载和项目装配分离为私有 helper 模块。
- [ ] 保留 `editor.service.ts` 和 `table.service.ts` 作为 feature 边界，但补充实际语义，例如错误文案、保存前 normalize 或上下文约束。
- [ ] 明确 UI 和文档中的保存边界：顶部保存只保存 CSV，编辑器保存只保存 `.ship/.wpn/.proj`。
- [ ] 评估是否需要 `.ship` 字段和 CSV 字段联动；如需要，先设计同步规则再实现。
- [ ] 为后端项目加载拆分后的 faction/projectile/sprite 路径补最小测试或验证样例。

## Phase 4: 结束迁移状态并删除旧程序

- [ ] 彻底删除 `old_program/` 目录。
- [ ] 从 `AGENTS.md`、`README.md`、`.trae/` 文档中移除 `old_program` 引用。
- [ ] 从项目文档中移除“旧版参考”“迁移参考”等历史叙述。
- [ ] 清理配置和脚本中针对 `old_program/` 的 ignore 或跳过逻辑。
- [ ] 验证删除后编码检查、格式检查、前端检查和 Rust 检查仍通过。

## Phase 5: 重新整理项目 CSS

- [ ] 明确哪些样式保留为 global token、reset 和布局基础。
- [ ] 决定模块样式按页面聚合还是按 feature 聚合。
- [ ] 拆分或重组当前 `src/styles.css`，避免继续膨胀。
- [ ] 建立颜色、间距、边框、阴影、圆角的基础 token。
- [ ] 验证表格、右侧面板、弹窗、编辑器滚动区域不回归。

## Phase 6: 重新设计前端界面（允许重构语义）

- [ ] 定义 Native UI + 现代圆角 + Notion 风格极简的视觉基准。
- [ ] 重设主窗口导航、顶部栏、数据表和右侧详情面板。
- [ ] 重设舰船、武器、弹丸编辑器弹窗布局。
- [ ] 重设颜色、层级、hover、selected、dirty、disabled 状态。
- [ ] 验证高密度数据工具场景下可读性和操作效率。

## Phase 7: 重新梳理项目链路/确认板块

- [ ] 确认应用内模块分块是否合理：project、tables、editors、preview。
- [ ] 确认弹窗、抽屉、右侧详情面板之间的职责边界。
- [ ] 梳理打开项目、切换表、选择记录、打开编辑器、保存的完整链路。
- [ ] 确认 store、service、component、composable 是否仍符合稳定语义。
- [ ] 删除或合并不再提供维护价值的中间层。

## Phase 8: 重新梳理快捷键

- [ ] 参考 `sf-ship-ed` 的快捷键习惯，整理舰船编辑器快捷键。
- [ ] 参考 `sf-ship-ed` 的快捷键习惯，整理武器编辑器快捷键。
- [ ] 为弹丸编辑器和弹道预览定义必要快捷键。
- [ ] 在不增加学习成本的前提下统一撤销、重做、保存、关闭等通用行为。
- [ ] 避免快捷键和输入框、文本域、系统快捷键冲突。

## Phase 9: 定义右键行为

- [ ] 定义主表格右键菜单：复制 ID、打开编辑器、删除、定位资源等。
- [ ] 定义舰船画布右键行为：添加点、删除点、切换模式、复制坐标等。
- [ ] 定义武器画布右键行为：添加 barrel、删除 barrel、复制坐标等。
- [ ] 定义弹丸编辑器右键行为：复制字段、重置字段、定位贴图等。
- [ ] 确保右键菜单不会破坏画布右键拖动平移体验。
